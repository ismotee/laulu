#pragma once

#include <vector>

float keskiarvo(const std::vector<float>& tiedot);
float keskiarvo(const std::vector<float>& tiedot, int otanta);
float varianssi(const std::vector<float>& tiedot);
float keskihajonta(const std::vector<float>& tiedot);

float varianssi(const std::vector<float>& tiedot, int otanta);
float keskihajonta(const std::vector<float>& tiedot, int otanta);