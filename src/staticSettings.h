#pragma once

// work around for multi window ofGetWidth and ofGetHeight functions
// remember to refresh these when resizing

static float windowWidth = 1650;
static float windowHeight = 1050;
static float monitorWidth = 1920;
static float monitorHeight = 1080;