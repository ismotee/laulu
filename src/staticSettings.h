#pragma once

// work around for multi window ofGetWidth and ofGetHeight functions
// remember to refresh these when resizing

static float windowWidth = 1280;
static float windowHeight = 700;
static float monitorWidth = 640;
static float monitorHeight = 400;